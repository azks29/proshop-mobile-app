import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AdminScren = () => {
  return (
    <View>
      <Text>AdminScren</Text>
    </View>
  );
};

export default AdminScren;

const styles = StyleSheet.create({});
